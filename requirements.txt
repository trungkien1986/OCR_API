# Khung API + hàng đợi
fastapi==0.115.*
uvicorn[standard]==0.32.*
python-multipart==0.0.*
rq==2.*
redis==5.*
pydantic==2.*
pydantic-settings==2.*

# DB + migration (Postgres)
sqlalchemy==2.*
alembic==1.*
psycopg[binary]==3.*

# Gửi webhook SSRF-safe (pin IP, giữ SNI/Host đúng domain)
urllib3==2.*
certifi>=2024.0

# Mã hoá tenant_secret lúc lưu trữ (Fernet)
cryptography==43.*

# OCR (lang="vi") + nhận diện bảng (PP-StructureV3) — benchmark chính thức PaddleOCR vs
# VietOCR trên golden dataset vẫn dời đúng lịch Tuần 9-10 (xem OCR_ENGINE_DESIGN.md Mục 6,
# Mục 13); PaddleOCR là lựa chọn mặc định cho tới lúc đó.
paddlepaddle==3.3.*
paddleocr[doc-parser]==3.7.*

# Đọc PDF: phát hiện text layer sẵn, rasterize trang thành ảnh cho OCR/nhận diện bảng
pymupdf==1.28.*

# Rule engine validate nghiệp vụ cấu hình YAML (design/pipeline.md Mục 8.6)
pyyaml==6.*

# Fuzzy-match tên chỉ tiêu OCR được với danh mục mã số chuẩn (Mục 8.2)
rapidfuzz==3.*
